# BioDesigner — front-end

Next.js renderer, built as a static export.

```bash
npm install
npm run dev
```

A two-column grid: the navbar on the left, the `Generation` pane on the right.
Both are markup only — no state, no data, no API calls.
