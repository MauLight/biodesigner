# Where the back-end is listening. Inlined at build time, so it must be
# NEXT_PUBLIC_-prefixed to reach the browser.
#
# Copy this file to .env.local. Next loads .env files from this directory, not
# from src/.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
